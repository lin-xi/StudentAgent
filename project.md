需求描述：开发一个带图形用户界面的智能学习助手示例程序，前端使用 Vue 3 的 Composition API（setup 语法）和 IndexedDB 进行本地数据持久化，后端使用 LangChain 构建智能 Agent（提供题目生成与答案评估服务）。具体功能要求如下：

1. 进度恢复与初始化

- 应用启动时，首先从 IndexedDB 中读取用户的学习进度数据。

- 如果存在历史进度（即用户已选择科目和年级，并且至少进行过一次答题），则直接在主界面展示当前用户所选的科目、年级以及各个大纲知识点的学习状态（包括已完成通关的知识点、当前正在攻克的知识点及其难度等级）。

- 如果没有历史进度（首次使用或进度已清空），则界面中央显示一个“开始学习”按钮。点击该按钮后，进入科目与年级选择流程（即第 2 步）。

2. 选择科目

- 提供三个科目选项：语文、数学、英语。

- 用户必须选择一个科目，且选择后不可在该轮学习流程中更改（除非重置进度）。

3. 选择年级

- 提供一年级至六年级（或可根据实际学段调整，例如增加七至九年级）的选项。

- 用户选择一个年级后，确认选择并进入下一步。

4. 课程大纲与三阶段测评

- 根据用户选择的科目和年级，系统自动获取对应的课程大纲。大纲由三个核心知识点组成，每个知识点均细分为三个难易程度：基础（Basic）、进阶（Intermediate）、高阶（Advanced）。

- 用户首先面对第一个知识点的“基础”难度题目。系统通过 LangChain Agent 动态生成一道与该知识点和难度匹配的题目（例如选择题或判断题）。

- 用户作答后，Agent 对答案进行评测，判定是否“通过”。

- 通过判定规则：若回答正确，则认为用户掌握了当前难度内容，允许晋级到同一知识点的下一个难度（基础→进阶→高阶）；若回答错误，则判定为未掌握，此时不允许进入下一难度，而是停留在同一个难度，由 Agent 重新生成另一道相同难度但不同内容的题目，直到用户答对该难度的题目为止。

- 当用户依次通过某个知识点的全部三个难度（基础、进阶、高阶）后，即视为完整掌握该知识点，该知识点标记为“已通关”。

- 随后自动解锁下一个知识点，并从其“基础”难度开始继续上述流程。

- 系统需要实时记录每个知识点的每个难度是否已通过，并持久化到 IndexedDB。

- 当三个知识点全部通关后，表示当前年级的学习任务完成。

5. 等级与关卡系统

- 整体设计一套基于关卡进度的等级称号。每个知识点视为一个“关卡”，每个知识点包含的三个难度视为关卡内的三个小阶段。

- 每通过一个知识点（即其三个难度全部通关），即可点亮对应的关卡徽章。

- 根据累计通关的知识点数量，为用户赋予不同的等级称号（例如：青铜学员：通关 1 个知识点；白银学员：通关 2 个知识点；黄金学员：通关 3 个知识点；可进一步扩展为更高等级）。

- 在 UI 上以可视化方式展示关卡地图或进度条，清晰显示每个知识点的通关状态以及当前正在挑战的难度。

6. 前端技术要求

- 使用 Vue 3 的 <script setup> 语法编写组件。

- 使用 IndexedDB（可通过 idb 库封装）存储用户进度，存储内容包括：当前科目、当前年级、每个知识点的各难度通过状态、当前正在进行的知识点与难度、以及整体通关记录。

- 页面应具备良好的响应式布局，适配不同屏幕尺寸。

- 需要实现进度自动保存：每次用户答对题目（通过一个难度）后，立即更新 IndexedDB 中的状态，确保刷新页面后进度不丢失。

7. 后端 LangChain Agent 要求

- 后端提供一个 HTTP API（例如使用 FastAPI），至少包含两个端点：

  - 出题端点：接收科目、年级、知识点名称、难度等级，调用 LangChain 的 LLM 链（如 GPT-3.5/4）生成一道合适的题目，并返回题目文本、选项（如果是选择题）及标准答案。

  - 评测端点：接收科目、年级、知识点、难度、题目内容、用户答案和标准答案，使用 LangChain 的评估链判断答案是否正确，并返回是否“通过”。

- Agent 应具备一定的鲁棒性：当 LLM 服务不可用时，可使用本地规则或预置题库作为降级方案。

- 后端需支持跨域请求（CORS），以便前端调用。

8. 用户体验增强点（可选但推荐）

- 在用户作答后，给出明确的“正确/错误”反馈，并显示正确答案及简短解析。

- 每个难度连续失败多次（例如3次）时，可给出提示或建议复习的内容。

- 提供“重置进度”按钮，允许用户清除 IndexedDB 中的记录并重新开始。

- 展示当前等级称号的进度百分比或下一等级所需通关的知识点数。
